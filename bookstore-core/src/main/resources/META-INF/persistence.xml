<?xml version="1.0" encoding="UTF-8"?>

<persistence version="2.2"
     xmlns="http://xmlns.jcp.org/xml/ns/persistence"
     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
     http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">
	
	<!-- 
	This persistence unit is actually never used, but if removed will give
	a warning because there's 2 persistence units in the test resource,
	even if JPA 2.2 supports multiple units.
	 -->
	<persistence-unit
		name="apt.project.bookstore.development"
		transaction-type="RESOURCE_LOCAL">

		<provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
		
		<class>piero.aldinucci.apt.bookstore.model.Book</class>
		<class>piero.aldinucci.apt.bookstore.model.Author</class>
		
		<properties>
			<property name="javax.persistence.schema-generation.database.action" value="none"/>
			<property name="javax.persistence.create-database-schemas" value="false"/>
		
			<property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver" />
			<property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQL95Dialect"/>
			<property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/projectAPTTestDb" />
			
			<property name="hibernate.show_sql" value="false" /> <!-- show SQL logs -->
			<property name="hibernate.format_sql" value="true" /><!--  format the SQL to make it readable -->
			
		</properties>

	</persistence-unit>
</persistence>